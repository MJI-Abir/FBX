class Cloud {
  // Function to update player status in the cloud account
  void updateStatus(String playerName, String status) {
    print("Updating $playerName's status in the cloud account to: $status");
  }
}
